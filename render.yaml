services:
  - type: web
    name: federated-search
    env: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/federatedsearch-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JDK_VERSION
        value: "17"
